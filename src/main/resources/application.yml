cloud:
  aws:
    credentials:
      access-key: 엑세스키
      secret-key: 시크릿키
    region:
      static: ap-northeast-2
    stack:
      auto: false

application:
  bucket:
    name: 버킷이름

spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB
